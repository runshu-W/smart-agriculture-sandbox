# AGENTS.md

- 本仓库是从 0 开始开发的智慧农业虚拟仿真实训沙盘项目。
- `docs\unit_details` 是每个单元唯一需求真相源，开始每个单元的开发前先读取相关文档。
- 按“规划 → 需求拆解 → 素材规划 → 素材生成 → 实现 → 验证”开发
- 每开发一个单元前，必须先输出该单元的：
  - `UNIT_SPEC.md`
  - `ANIMATION_SPEC.md`
  - `ASSET_MANIFEST.md`
  - `ASSET_PROMPTS.md`
- 素材规划时必须判断其是否可跨单元复用；可复用素材优先纳入全局素材库，不得在各单元重复生成。
- 全局素材统一记录在 docs/assets/，包括清单、生成提示词、角色风格规范和复用关系。
- 所有需要透明背景的图片素材，优先走 `transparent-visual-assets` 流程。
- 每次生成素材后，必须记录素材文件、用途、生成提示词和保存位置。
- 不得将文档中的动态场景、动画、拖拽、反馈、分支逻辑默默降级为普通选择题或静态卡片。
- 所有关卡交互都必须接入统一事件模型，供学生端与教师端数据看板复用。
- 数据看板属于核心需求，不得后置为“可选优化”。
- 缺失素材时允许使用明确占位，但必须同步更新素材清单。
- 完成任务前必须运行 lint、typecheck、test、build，并汇报结果。