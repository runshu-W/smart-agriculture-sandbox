# Local development uses the `smart-v2` Prisma Dev database.
# tools/windows/install.cmd creates .env automatically.
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:51214/template1"

# Keep Prisma Dev on one connection, including npm run start.
DATABASE_POOL_MAX=1
