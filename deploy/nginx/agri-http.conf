# Certificate bootstrap only. Enable HTTPS config after the certificate exists.
server {
    listen 80;
    listen [::]:80;
    server_name agri.voyageage.com;
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
    }
    location / { return 404; }
}
