"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { Archive, KeyRound, Plus, RotateCcw, Trash2, UserPlus, UsersRound } from "lucide-react";

type Member = { id: string; userId: string; status: "ACTIVE" | "ARCHIVED"; user: { id: string; username: string; displayName: string; nickname: string | null; studentNo: string | null; status: string } };
type ClassItem = { id: string; classId: string; classRoom: { id: string; name: string; academicYear: string; semester: string; isArchived: boolean; leaderboardAnonymous: boolean; enrollments: Member[] } };

async function send(url: string, method: string, body?: object) {
  const response = await fetch(url, { method, headers: { "Content-Type": "application/json" }, ...(body ? { body: JSON.stringify(body) } : {}) });
  const data = await response.json(); if (!response.ok) throw new Error(data.error ?? "操作失败"); return data;
}

export function ClassManager({ classes }: { classes: ClassItem[] }) {
  const router = useRouter(); const [selected, setSelected] = useState(classes[0]?.classId ?? ""); const [message, setMessage] = useState(""); const selectedClassId = classes.some((item) => item.classId === selected) ? selected : classes.find((item) => !item.classRoom.isArchived)?.classId ?? classes[0]?.classId ?? ""; const current = classes.find((item) => item.classId === selectedClassId)?.classRoom;
  async function run(action: () => Promise<unknown>, onSuccess?: (result: unknown) => void) { try { setMessage("处理中…"); const result = await action(); onSuccess?.(result); setMessage("操作已保存"); router.refresh(); } catch (error) { setMessage(error instanceof Error ? error.message : "操作失败"); } }
  return <div className="class-manager"><section className="class-list-panel"><header><h2>我的班级</h2><span>{classes.length} 个</span></header>{classes.map((item) => <button className={selected === item.classId ? "active" : ""} key={item.classId} onClick={() => setSelected(item.classId)}><b>{item.classRoom.name}</b><small>{item.classRoom.academicYear} · {item.classRoom.semester}</small><span>{item.classRoom.enrollments.filter((member) => member.status === "ACTIVE").length} 人{item.classRoom.isArchived ? " · 已归档" : ""}</span></button>)}<details><summary><Plus />新建班级</summary><form onSubmit={(event) => { event.preventDefault(); const form = new FormData(event.currentTarget); void run(() => send("/api/teacher/classes", "POST", Object.fromEntries(form)), (result) => { if (result && typeof result === "object" && "id" in result && typeof result.id === "string") setSelected(result.id); }); }}><input name="name" placeholder="班级名称" required /><input name="academicYear" defaultValue="2026-2027" required /><input name="semester" defaultValue="第一学期" required /><button className="primary-button">创建</button></form></details></section>{current ? <section className="member-manager"><header><div><h2>{current.name}</h2><p>{current.academicYear} · {current.semester}</p></div><div><button onClick={() => void run(() => send(`/api/teacher/classes/${current.id}`, "PATCH", { leaderboardAnonymous: !current.leaderboardAnonymous }))}>{current.leaderboardAnonymous ? "排行榜匿名：开" : "排行榜匿名：关"}</button><button className="danger-quiet" onClick={() => void run(() => send(`/api/teacher/classes/${current.id}`, "PATCH", { isArchived: true }))}><Archive />归档班级</button></div></header><details className="add-member"><summary><UserPlus />添加学生</summary><form onSubmit={(event) => { event.preventDefault(); const form = new FormData(event.currentTarget); void run(() => send(`/api/teacher/classes/${current.id}/members`, "POST", Object.fromEntries(form))); }}><input name="displayName" placeholder="学生姓名" required /><input name="studentNo" placeholder="学号" required /><input name="username" placeholder="登录账号" required /><input name="nickname" placeholder="匿名昵称（可选）" /><button className="primary-button">添加成员</button></form></details><div className="admin-table member-table"><div className="admin-table-head"><span>姓名</span><span>学号</span><span>账号</span><span>昵称</span><span>状态</span><span>操作</span></div>{current.enrollments.map((member) => <div key={member.id}><span><i className="student-avatar">{member.user.displayName.slice(-1)}</i><b>{member.user.displayName}</b></span><span>{member.user.studentNo}</span><span>{member.user.username}</span><span>{member.user.nickname ?? "未设置"}</span><b className={member.status === "ACTIVE" ? "status-active" : "status-archived"}>{member.status === "ACTIVE" ? "在班" : "已移出"}</b><span className="row-actions"><button title="重置密码" onClick={() => void run(() => send(`/api/teacher/classes/${current.id}/members/${member.userId}`, "PATCH", { action: "reset-password" }))}><KeyRound /></button>{member.status === "ACTIVE" ? <button title="移出班级" onClick={() => { if (window.confirm(`确认将${member.user.displayName}移出班级？学习记录会保留。`)) void run(() => send(`/api/teacher/classes/${current.id}/members/${member.userId}`, "DELETE")); }}><Trash2 /></button> : <button title="恢复成员" onClick={() => void run(() => send(`/api/teacher/classes/${current.id}/members/${member.userId}`, "PATCH", { action: "restore" }))}><RotateCcw /></button>}</span></div>)}</div>{message && <p className="manager-message" role="status">{message}</p>}</section> : <section className="member-manager admin-empty"><UsersRound /><b>尚未建立班级</b><p>请先在左侧创建班级，再添加学生。</p></section>}</div>;
}
